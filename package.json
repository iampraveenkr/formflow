{
  "name": "formflow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Next.js dev server is scaffolded but dependency install is required.'",
    "build": "npm run typecheck",
    "start": "echo 'Use npm run dev after installing dependencies in a network-enabled environment.'",
    "lint": "npm run typecheck",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/navigation.test.mjs",
    "format": "echo 'Prettier config is included; run prettier in a fully provisioned environment.'",
    "format:check": "echo 'Prettier config is included; run prettier in a fully provisioned environment.'"
  },
  "dependencies": {},
  "devDependencies": {}
}
